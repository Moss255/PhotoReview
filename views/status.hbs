<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Image Viewer with Buttons</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <style>
    body {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background-color: #f5f5f5;
    }
    .card {
      width: 100%;
      max-width: 500px;
    }
    .card-image img {
      width: 100%;
      height: auto;
    }
    .card-action {
      display: flex;
      justify-content: space-around;
    }
  </style>
</head>
<body>

  <div class="card">
    <div class="card-header">
          <span class="card-title">Is this cursed?</span>
    </div>
    <div class="card-image">
      <img src="{{photo}}" alt="Placeholder Image">

    </div>
    <div class="card-action">
    {{#if isPending}}
      <h5 class="grey-text text-darken-2">Your photo is pending Judgement</h5>
    {{/if}}
    {{#if isYes}}
        <h5 class="grey-text text-darken-2">Your photo was determined to be <b>Cursed</b></h5>
    {{/if}}
    {{#if isNo}}
        <h5 class="grey-text text-darken-2">Your photo was determined to be <b>Not Cursed</b></h5>
    {{/if}}
    </div>
  </div>

  <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>

</body>
</html>